{
  "name": "collabstr-scraper",
  "version": "1.0.0",
  "description": "Chrome extension to scrape Collabstr influencer profiles",
  "scripts": {
    "start": "echo Extension ready - load in Chrome",
    "api": "cd api && npm install && npm start",
    "api:dev": "cd api && npm install && npm run dev"
  },
  "dependencies": {},
  "devDependencies": {},
  "private": true
}
